pre-commit:
  parallel: true
  commands:
    fmt-check:
      glob: "*.{rs}"
      run: cargo fmt --all -- --check
    fmt-fix:
      glob: "*.{rs}"
      run: cargo fmt --all
      stage_fixed: true
    clippy:
      glob: "*.{rs}"
      run: cargo clippy --all-targets -- -D warnings
