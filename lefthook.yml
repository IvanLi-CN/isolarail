pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.{rs}"
      run: cargo +esp fmt
      stage_fixed: true
    clippy:
      glob: "*.{rs}"
      run: cargo +esp clippy --target xtensa-esp32s3-none-elf
    markdown-lint:
      glob: "**/*.md"
      run: bunx markdownlint-cli2 {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
